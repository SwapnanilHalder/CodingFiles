#include<bits/stdc++.h>
#define SPEED ios_base::sync_with_stdio(false);cin.tie(0);cout.tie(0)
#define endl '\n'
#define vi vector<int>
#define pb push_back
#define For(i,a,b) for(int i=a;i<b;i++)
#define ford(i,a,b) for(int i=a;i>=b;i--)
#define pi 3.1415926536
#define END cout<<"\n"
using namespace std;
  
int main() 
{ 
    cout<< (5 << 1)<< "   " << (5 >> 1); // Shifting the whole number one step left, == *2
    END;
    cout << (1^1^2^4^5^4^9^5^2) ; //sum of Unique Numbers in pair wise array
    END;
    cout<< (6&1)<< "  "<< (1019529 & 1); // If returns 1, its odd number, else even
    END;
    cout<< (4 & 3);
    END;
    int a = 5, b = 7; // Swapping using XOR
    a = a^b;
    b = a^b;
    a = a^b;
    cout<< a << "  " << b<<endl;
    cout << (1^1^2^3) << endl;
    cout << (1<<5) << endl; //  (2 to the power 5)
}